<UserControl x:Class="CMMDemoApp.Views.Panels.FeaturesPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="Auto"/>
            <!-- для GridSplitter -->
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Верхняя часть -->
        <TreeView Grid.Row="0" Margin="4">
            <TreeViewItem Header="Assembly">
                <TreeViewItem Header="Part A"/>
                <TreeViewItem Header="Part B"/>
            </TreeViewItem>
        </TreeView>

        <!-- Разделитель -->
        <GridSplitter Grid.Row="1"
                      Height="4"
                      HorizontalAlignment="Stretch"
                      VerticalAlignment="Center"
                      Background="#DDD"
                      ResizeDirection="Rows" />

        <!-- Нижняя часть -->
        <TabControl Grid.Row="2" Margin="4">
            <TabItem Header="Details">
                <TextBlock Text="Здесь подробности по элементу" />
            </TabItem>
            <TabItem Header="Notes">
                <TextBlock Text="Комментарии, аннотации и пр." />
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
